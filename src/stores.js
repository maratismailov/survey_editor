import { writable } from 'svelte/store';

export const store_survey = writable(0);
export const store_current_element = writable(0)
export const store_blank_element = writable(0)
export const store_current_index = writable(0)
export const store_dictionary = writable(0)

